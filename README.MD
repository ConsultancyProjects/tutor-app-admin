Change IP in prod env file if java ip changes

ng build --prod
Upload all files and folders in dist/tutor-app

Add bucket policy , check existing bucket
Make bucket public
In property enable static website hosting
